import * as Types from '../actions/Types'

const initialState = {
  events: []
}

export default (state = initialState, action) => {
  switch(action.type) {
    default:
      return initialState
  }
}